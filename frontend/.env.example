# Copie para .env.local (Next.js carrega automaticamente em dev/build local)
# No Railway: defina em Variables do serviço frontend (sem prefixo de arquivo)

# URL base da API FastAPI — deve terminar em /api/v1 (igual ao backend)
NEXT_PUBLIC_API_URL=http://localhost:8000/api/v1
